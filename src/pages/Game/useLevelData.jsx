const useLevelData = (levelNum) => {
  return {
    data: {
      token: "token",
      img: "#",
      characters: ["Wally", "Woof", "Odlaw"],
    },
    loading: false,
    error: null,
  };
};

export default useLevelData;
